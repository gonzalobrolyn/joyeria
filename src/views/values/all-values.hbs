<div class="row">
  {{#if values}}
    <div class="card w-100">
      <div class="card-body">
        <h1 class="card-title display-4 d-flex justify-content-between align-items-center">Lista de Valoraciones 
          <a href="/values/add" class="d-flex justify-content-center">
            <i class="fas fa-plus-square" style="color: #2780e3 "></i>
          </a>
        </h1>
        <table class="table table-hover table-sm">
          <thead>
            <tr>
              <th scope="col">Valoración</th>
              <th scope="col">Precio</th>
              <th scope="col">Editar</th>
              <th scope="col">Eliminar</th>
            </tr>
          </thead>
          <tbody>
            {{#each values}}
              <tr>
                <td>{{nombre}}</td>
                <td>S/ {{precio}}</td>
                <td>
                  <a href="/values/edit/{{_id}}" class="d-flex justify-content-center">
                    <i class="fas fa-edit"></i>
                  </a>
                </td>
                <td>
                  <a href="/values/delete/{{_id}}" class="d-flex justify-content-center">
                    <i class="fas fa-trash-alt" style="color: red;"></i>
                  </a>
                </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  {{else}}
    <div class="card mx-auto">
      <div class="card-body">
        <p class="lead">No hay valoraciones</p>
        <a href="/values/add" class="btn btn-success btn-block">Registrar una valoración</a>
      </div>
    </div>
  {{/if}}
</div>